from enum import Enum


class AggregationEnum(Enum):
    P1H = "P1H"  # Hourly aggregation
    # P1D = "P1D"  # Daily aggregation
    # P1M = "P1M"  # Monthly aggregation
    # P1Y = "P1Y"  # Yearly aggregation
