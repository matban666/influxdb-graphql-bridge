from django.apps import AppConfig


class ServiceConfig(AppConfig):
    """
    Configuration class for the 'service' app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'service'
